# 2026-07-07 工作日志

- 为 Railway 部署准备配置文件：
  - `start.sh`：启动脚本，首次运行自动构建向量索引，然后启动 Streamlit
  - `Dockerfile`：基于 python:3.10-slim，安装依赖 + 复制代码 + 执行 start.sh
  - `.dockerignore`：排除面试文档、索引文件、.env 等
  - `.gitignore` 修复：移除了过于宽泛的 `*.json` 规则，改为仅排除 `doc_emb/` 和 `docs_emb/`
  - `.streamlit/config.toml`：添加 headless=true、gatherUsageStats=false 等生产配置
- 部署策略：数据文件（537KB）随仓库上传，索引文件（96MB）在首次启动时自动构建
- 环境变量：DASHSCOPE_API_KEY 通过 Railway 控制台设置
